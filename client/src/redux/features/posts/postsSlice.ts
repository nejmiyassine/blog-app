export {};
// import { createSlice } from '@reduxjs/toolkit';
// import type { PayloadAction } from '@reduxjs/toolkit';
// import { PostsData } from '../../api/postsApi';
// interface IPosts {
//   posts: PostsData | null;
// }

// export const initialState: IPosts = {
//   posts: null,
// };

// export const postsSlice = createSlice({
//   name: 'posts',
//   initialState,
//   reducers: {
//     setPosts: (state, action: PayloadAction<PostsData>) => {
//       state.posts = action.payload;
//     },
//     deletePosts: (state, action: PayloadAction<PostsData>) => {
//       state.posts = action.payload;
//     },
//   },
// });

// export const { setPosts, deletePosts } = postsSlice.actions;

// export default postsSlice.reducer;
